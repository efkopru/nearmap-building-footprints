# Local input data

Place imagery, prompt vectors, complete reference labels, evaluation AOIs, and prepared tiles here, or point the CLI to another local location. No real imagery or reference data is included. Contents are ignored by Git. Record imagery capture date, GSD, CRS, label target (roof or ground footprint), and provenance with each experiment.
